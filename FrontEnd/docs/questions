Граматика:
    Правильно ли я понимаю, что язык программрования - это язык инициализаций переменных и функций.
    Но есть нюанс: существует особенная функция int main().
    Ее func_id(main) заранее занесен в name_table (создаю name_table в BackEnd)
    Типы возвращаемых значений и принимаемых аргументов тоже заранее прописаны в name_table

    В Backend в процессе кодогенерации проинициализируются глобальные переменные, функции,
    будет транслированы тела функций (с учетом меток, названий)

    Перед hlt будет вызов функции main. (При остутствии main выдавать ошибку)

